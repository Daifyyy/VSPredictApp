import{describe,expect,it}from"vitest";import type{Metric,MetricValue,Venue}from"@/lib/types";import{buildPerformancePressureShadowV5}from"./performancePressureShadowV5";
const mv=(metric:Metric,venue:Venue,value:number):MetricValue=>({metric,venue,value,sampleSize:12,lowConfidence:false,breakdown:[]});const p=(venue:Venue,shots:number,allowed:number)=>[mv("SHOTS",venue,shots),mv("SHOTS_ON_TARGET",venue,shots*.33),mv("SHOTS_INSIDE_BOX",venue,shots*.64),mv("XG",venue,shots*.105),mv("XG_AGAINST",venue,1.3),mv("SHOTS_AGAINST",venue,allowed),mv("SHOTS_ON_TARGET_AGAINST",venue,allowed*.33),mv("SHOTS_INSIDE_BOX_AGAINST",venue,allowed*.64),mv("POSSESSION",venue,50),mv("CORNERS",venue,4)];
describe("pressure v5",()=>{it("uses opponent allowed volume without breaking metric coherence",()=>{const r=buildPerformancePressureShadowV5({homeValues:p("HOME",14,8),awayValues:p("AWAY",8,18),currentLambdaHome:1.7,currentLambdaAway:.8,currentOver25:.5});expect(r.version).toBe(5);expect(r.allowedVolumeCoverage).toBe(1);expect(r.expectedMatchShape.home.shots.value).toBeGreaterThan(14);expect(r.expectedMatchShape.home.shotsOnTarget.value).toBeLessThanOrEqual(r.expectedMatchShape.home.shots.value!)});it("falls back cleanly when allowed metrics are absent",()=>{const bare=p("HOME",12,12).filter(v=>!v.metric.endsWith("_AGAINST")||v.metric==="XG_AGAINST");const r=buildPerformancePressureShadowV5({homeValues:bare,awayValues:bare,currentLambdaHome:1.2,currentLambdaAway:1.1,currentOver25:.45});expect(r.allowedVolumeCoverage).toBe(0);expect(r.expectedMatchShape.home.shots.value).toBeGreaterThan(0)})});
