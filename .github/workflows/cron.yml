# Plánované úlohy (crony) appky.
#
# **Proč GitHub Actions a ne `vercel.json`:** Vercel Hobby plán dovolí jen **2 cron
# joby a jen denní** frekvenci. Úloh máme šest a snímky kurzů potřebují běžet **hodinově**
# – jinak zavírací snímek dostanou jen zápasy s výkopem mezi 04:30 a 16:30 UTC
# (viz `ODDS_CLOSING_HOURS` v lib/data/predictions.ts) a CLV by stálo na vychýlené
# menšině. Actions to zvládnou zdarma a v libovolné frekvenci.
#
# Nastavení (jednorázově, v GitHubu → Settings → Secrets and variables → Actions):
#   - secret   CRON_SECRET  … stejná hodnota jako env proměnná na Vercelu
#   - variable APP_URL      … https://statapp-uvol.vercel.app  (bez koncového lomítka)
#
# Pozn.: `schedule` v Actions je **best-effort** – při zátěži se spuštění opozdí
# i o pár desítek minut, občas se běh nespustí vůbec. Denním úlohám to nevadí.
# U snímků kurzů je proto okno (`ODDS_CLOSING_HOURS = 3 h`) záměrně **širší než
# perioda běhu (1 h)**: zápas tak má na zavírací snímek tři pokusy a jeden výpadek
# ho nepřipraví o CLV. Časy jsou v **UTC**.

name: Cron

on:
  schedule:
    # Snímky kurzů – nejčastější úloha, kvůli zavírací linii u večerních zápasů.
    # HODINOVĚ (od 28. 7. 2026): `fixturesNeedingOdds` bere první běh uvnitř okna, takže
    # při běhu po 3 h a 12h okně padal „zavírací" snímek 9–12 h před výkopem a CLV mu
    # unikal celý poslední den. Viz `ODDS_CLOSING_HOURS` (dnes 3 h) – ta dvě čísla musí
    # sedět dohromady: kratší okno bez častějšího běhu = zápasy bez zavíracího snímku.
    # Minuta 20 schválně: denní úlohy jedou na :00, :15 a :30, takže nikdy nenastane
    # okamžik, kdy by seděly dva rozvrhy naráz.
    - cron: "20 * * * *"
    - cron: "7,22,37,52 * * * *" # Web Push připomínky oblíbených zápasů
    - cron: "50 */3 * * *" # refresh-referees
    # Denní údržba (časy odpovídají původnímu vercel.json).
    - cron: "0 3 * * *" # refresh-league-styles
    - cron: "0 4 * * *" # warm
    - cron: "30 4 * * *" # predict-upcoming
    - cron: "0 5 * * *" # refresh-transfers
    # Vypořádání jede DVAKRÁT denně (ráno + večer). S jediným ranním během čekal zápas
    # s výkopem v 18:00 UTC na ✓/✗ ~17 h – a protože `schedule` v Actions se opožďuje,
    # v praxi to bylo až 08:30 UTC následujícího dne. Večerní běh chytí i pozdní zápasy
    # (grace `getUnsettledPredictions` = 3 h → výkop 18:00 UTC je v 22:00 UTC zralý).
    # Skóre samotné na tomhle nestojí – to jde do Výsledků z denního rozpisu –, ale
    # odznak „trefili jsme to" ano. Cena je pár volání (`fetchFixturesByIds` po 20).
    - cron: "0 6 * * *" # settle-results
    - cron: "15 6 * * *" # settle-tips
    - cron: "0 22 * * *" # settle-results (večerní zápasy týž den)
    - cron: "15 22 * * *" # settle-tips
  # Ruční spuštění (Actions → Cron → Run workflow), např. po výpadku.
  workflow_dispatch:
    inputs:
      job:
        description: "Kterou úlohu spustit ručně"
        required: true
        default: snapshot-odds
        type: choice
        options:
          - snapshot-odds
          - refresh-league-styles
          - warm
          - predict-upcoming
          - refresh-referees
          - refresh-transfers
          - settle-results
          - settle-tips
          - send-notifications

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Zvol endpoint podle času spuštění
        id: pick
        env:
          EVENT: ${{ github.event_name }}
          SCHEDULE: ${{ github.event.schedule }}
          MANUAL: ${{ inputs.job }}
        run: |
          # O větvi rozhoduje TYP UDÁLOSTI, ne prázdnost `inputs`. Ruční spuštění má
          # u `job` default `snapshot-odds`, takže kdyby se `inputs` naplnily i při
          # plánovaném běhu, spadly by do snapshot-odds VŠECHNY rozvrhy a denní úlohy
          # by nikdy neběžely – a job by přitom svítil zeleně.
          if [ "$EVENT" = "workflow_dispatch" ]; then
            if [ -z "${MANUAL:-}" ]; then
              echo "Ruční běh bez zvolené úlohy." >&2
              exit 1
            fi
            echo "path=${MANUAL}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          case "$SCHEDULE" in
            "20 * * * *")   path="snapshot-odds" ;;
            "7,22,37,52 * * * *") path="send-notifications" ;;
            "50 */3 * * *") path="refresh-referees" ;;
            "0 3 * * *")    path="refresh-league-styles" ;;
            "0 4 * * *")    path="warm" ;;
            "30 4 * * *")   path="predict-upcoming" ;;
            "0 5 * * *")    path="refresh-transfers" ;;
            "0 6 * * *")    path="settle-results" ;;
            "15 6 * * *")   path="settle-tips" ;;
            "0 22 * * *")   path="settle-results" ;;
            "15 22 * * *")  path="settle-tips" ;;
            # Neznámý rozvrh = někdo přidal `- cron:` a zapomněl na větev tady.
            # Radši hlasitě spadnout než tiše zavolat špatný endpoint.
            *) echo "Neznámý rozvrh: '$SCHEDULE' (událost: $EVENT)" >&2; exit 1 ;;
          esac
          echo "path=$path" >> "$GITHUB_OUTPUT"
          echo "Rozvrh '$SCHEDULE' → úloha $path"

      - name: Zavolej endpoint
        env:
          APP_URL: ${{ vars.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          JOB: ${{ steps.pick.outputs.path }}
        run: |
          if [ -z "$APP_URL" ]; then
            echo "Chybí proměnná APP_URL (Settings → Variables)." >&2
            exit 1
          fi
          # `warm` má jinou cestu než ostatní (není pod /cron/).
          if [ "$JOB" = "warm" ]; then url="$APP_URL/api/warm"; else url="$APP_URL/api/cron/$JOB"; fi
          if [ "$JOB" = "refresh-league-styles" ]; then
            failed=0
            index=0
            cold_start=$(( (10#$(date -u +%j) * 3) % 18 ))
            cold_next=$(( (cold_start + 1) % 18 ))
            cold_last=$(( (cold_start + 2) % 18 ))
            for league in 39 140 135 78 61 94 88 40 144 203 345 179 197 103 119 106 218 207; do
              cold=0
              if [ "$index" -eq "$cold_start" ] || [ "$index" -eq "$cold_next" ] || [ "$index" -eq "$cold_last" ]; then cold=1; fi
              url="$APP_URL/api/cron/refresh-league-styles?league=$league&cold=$cold"
              echo "→ $url"
              curl --fail-with-body --silent --show-error --location \
                   --max-time 90 --retry 1 --retry-delay 10 \
                   --header "Authorization: Bearer $CRON_SECRET" "$url" || failed=$((failed + 1))
              echo
              index=$((index + 1))
            done
            if [ "$failed" -gt 0 ]; then echo "$failed ligových snapshotů selhalo." >&2; exit 1; fi
            exit 0
          fi
          if [ "$JOB" = "predict-upcoming" ]; then
            failed=0
            # Běžné ligy/reprezentace a každý evropský pohár zvlášť. Samostatné requesty
            # dávají každé soutěži vlastní 60s rozpočet Vercelu; upsert je idempotentní.
            for target in "$url" "$url?league=2" "$url?league=3" "$url?league=848"; do
              echo "→ $target"
              curl --fail-with-body --silent --show-error --location \
                   --max-time 90 --retry 1 --retry-delay 10 \
                   --header "Authorization: Bearer $CRON_SECRET" "$target" || failed=$((failed + 1))
              echo
            done
            if [ "$failed" -gt 0 ]; then echo "$failed predikčních běhů selhalo." >&2; exit 1; fi
            exit 0
          fi
          echo "→ $url"
          # `--fail-with-body` = nenulový exit u 4xx/5xx, ale tělo se pořád vypíše,
          # takže je v logu vidět DŮVOD. Statusy, které sem chodí:
          #   401 = nesedí CRON_SECRET · 503 = CRON_SECRET není v env na Vercelu
          #   502 = běh doběhl, ale NIC nezvládl (viz `cronJson` v lib/cronResult.ts)
          # Dílčí výpadek (část lig selhala, zbytek prošel) zůstává 200 = zeleně, ale
          # počet je vidět v těle jako `errors` – schválně, ať se červená nedevalvuje.
          # Bez `--fail*` by curl skončil úspěchem i na 500 a úloha by selhávala tiše –
          # což je přesně ta třída chyb, která u kurzů stála rok dat.
          curl --fail-with-body --silent --show-error --location \
               --max-time 300 --retry 2 --retry-delay 30 \
               --header "Authorization: Bearer $CRON_SECRET" \
               "$url"
          echo
